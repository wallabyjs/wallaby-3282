{
  "name": "@vitest-split-projects/app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:unit": "vitest --project unit",
    "test:browser": "vitest --project browser"
  }
}
