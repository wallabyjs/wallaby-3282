{
  "name": "vitest-split-projects",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces",
    "test:unit": "npm run test:unit --workspaces",
    "test:browser": "npm run test:browser --workspaces"
  },
  "devDependencies": {
    "@vitest/browser": "^4.0.0",
    "@vitest/browser-playwright": "^4.0.0",
    "playwright": "^1.50.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
